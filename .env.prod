# =================================
# CONFIGURACIÓN DE PRODUCCIÓN
# =================================

# Django Configuration
DEBUG=False
# SECURITY WARNING: SECRET_KEY must be set as an environment variable in production
# NEVER commit the actual SECRET_KEY to version control
# Generate a secure key: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# Set via environment variable, Docker secrets, or secure secrets manager
SECRET_KEY=
# IMPORTANTE: Incluye aquí el dominio de tu backend si tienes uno
# Ejemplo: ALLOWED_HOSTS=api.tu-dominio.com,3.133.11.109,localhost,127.0.0.1
ALLOWED_HOSTS=3.133.11.109,localhost,127.0.0.1

# Database Configuration
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# MongoDB Configuration
MONGO_HOST=
MONGO_PORT=
MONGO_DB_NAME=arryn_products_production
MONGO_ROOT_USERNAME=arryn_admin
MONGO_ROOT_PASSWORD=super_secure_mongo_password_change_this
MONGO_CONNECTION_TIMEOUT=10000
# MONGODB_URL will be set by GitHub Actions

# Redis Configuration
REDIS_PASSWORD=super_secure_redis_password_change_this

# Cache Configuration  
CACHE_BACKEND=django.core.cache.backends.locmem.LocMemCache
CACHE_LOCATION=arryn-cache
CACHE_TIMEOUT=600
CACHE_MAX_ENTRIES=10000

# Rate Limiting Configuration
RATE_LIMIT_REQUESTS=1000
RATE_LIMIT_WINDOW=60

# CORS Configuration
# URLs del frontend en producción (CloudFront + Dominios personalizados)
CORS_ALLOWED_ORIGINS=https://arryn.app,https://www.arryn.app,https://d2objaapejmkqm.cloudfront.net,http://localhost:5173

# Logging Configuration
LOG_LEVEL=WARNING
LOG_FILE=/app/logs/django.log

# Performance Settings
RESPONSE_CACHE_TIMEOUT=600
REQUEST_LOG_SLOW_THRESHOLD=2.0

# Gunicorn Configuration
GUNICORN_WORKERS=4
GUNICORN_WORKER_CLASS=gevent
GUNICORN_WORKER_CONNECTIONS=1000
GUNICORN_MAX_REQUESTS=1000
GUNICORN_TIMEOUT=60

# Django Admin Super User
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@your-domain.com
DJANGO_SUPERUSER_PASSWORD=change_this_admin_password

# API Settings
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
API_VERSION=v1